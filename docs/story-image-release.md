# Story image release, September 8, 2026

PR 183 deployed application commit `cb2ae0014572b1f1054eb80a345e694ed29f73e2`.
[Production workflow 34225263905](https://github.com/LaykenV/public-parish/actions/runs/34225263905)
and independent `npm run smoke:production` passed. CI passed 637 tests and review
reported no major issues.

The three owner-supplied PNGs replaced the official-document images in production.
Their exact hashes are fixed in `convex/stories/ownerMedia.ts`. The owner approved
an explicit editorial exception with unverified rights recorded. It does not
establish a copyright license or rightsholder permission. Meta's original source
is identified; Boyce and SpaceX retain explicit attribution gaps.

Each environment ran three fresh independent reviews using the exact retained
images and saved accepted evidence. All six returned LIMITED with cosmetic
change assessments and no unsupported checks. No story drafting or retrieval
reran. New immutable versions retain identical draft hashes and source spans.
No new story update events were created. Repeating each correction returned the
same build without model work or duplicate publication.

Production checks confirmed all three image hashes, native dimensions, captions,
homepage image loads, direct story pages, updated social-image URLs and changed
ETags. An old ETag receives fresh HTML with the new image. Development desktop
and mobile widths loaded the images without horizontal overflow. A development
homepage screenshot confirmed the supplied renderings. Actual Facebook cache
refresh and production email checks remain with founder QA.

The six reviews cost $0.03856 in the estimated-use ledger, within the $2 batch
ceiling. Meta's initial development reservation was rejected before a model call;
the same batch allowance was reallocated sequentially. Both original allowance
ceilings were restored after completion without erasing charges. Provider invoices
were not independently reconciled. No mail was sent for these image changes.
