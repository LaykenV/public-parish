name: Verify

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]

concurrency:
  group: verify-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      VITE_CONVEX_URL: https://woozy-wren-227.convex.cloud
    steps:
      - name: Check out the pull request
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Use npm 11.6.2
        run: npm install --global npm@11.6.2

      - name: Install dependencies
        run: npm ci

      - name: Verify the application
        run: npm run verify

      - name: Install browser engines
        if: contains(github.event.pull_request.labels.*.name, 'development-certification')
        run: npx playwright install --with-deps chromium webkit

      - name: Check desktop and mobile development journeys
        if: contains(github.event.pull_request.labels.*.name, 'development-certification')
        run: npx playwright test

      - name: Check reading and chat presentation states
        if: contains(github.event.pull_request.labels.*.name, 'development-certification')
        run: npx playwright test --config playwright.fixtures.config.ts

      - name: Preserve parish feed screenshots
        if: always() && contains(github.event.pull_request.labels.*.name, 'development-certification')
        uses: actions/upload-artifact@v4
        with:
          name: parish-feed-${{ github.event.pull_request.head.sha }}
          path: |
            test-results/**/*.png
            test-results/**/*.webm
          if-no-files-found: ignore

      - name: Preserve browser failure evidence
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: browser-failure-${{ github.event.pull_request.head.sha }}
          path: test-results
          if-no-files-found: ignore

      - name: Preserve the verified development frontend
        uses: actions/upload-artifact@v4
        with:
          name: development-frontend-${{ github.event.pull_request.head.sha }}
          path: dist/client
          if-no-files-found: error
          retention-days: 14
